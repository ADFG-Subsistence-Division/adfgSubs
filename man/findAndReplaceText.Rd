% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/f_findAndReplaceText.R
\name{findAndReplaceText}
\alias{findAndReplaceText}
\title{Find and replace text in multiple files or folders}
\usage{
findAndReplaceText(
  files = NULL,
  folders = NULL,
  pattern = "\\\\.Rmd$",
  find,
  replace,
  fixed = TRUE,
  recursive = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{files}{Character vector of file paths to process. If \code{NULL}, files are determined from \code{folders}.}

\item{folders}{Character vector of folder paths. All files matching \code{pattern} within these folders (and subfolders, if \code{recursive = TRUE}) will be processed.}

\item{pattern}{Regular expression pattern for files to include (default: \code{"\\.Rmd$"}).}

\item{find}{Character vector of search strings.}

\item{replace}{Character vector of replacement strings (must be same length as \code{find}).}

\item{fixed}{Logical, use fixed (literal) matching if \code{TRUE} (default), or regular expressions if \code{FALSE}.}

\item{recursive}{Logical, if \code{TRUE} (default), search folders recursively.}

\item{verbose}{Logical, display messages for each file (default: \code{TRUE}).}
}
\value{
Invisibly returns a data frame detailing the status of each file processed.
}
\description{
Searches specified files or all files in given folders, replacing occurrences of search strings with replacement strings. Intended for batch processing of text across multiple files, such as R Markdown documents.
}
\examples{

findAndReplaceText(
  folders = c("docs"),
  find = c("oldtext1", "oldtext2"),
  replace = c("newtext1", "newtext2")
)

}
